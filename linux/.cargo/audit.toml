# cargo-audit config (project-local: linux/.cargo/audit.toml)
# Mirrors deny.toml advisory policy for CI.

[advisories]
ignore = [
    # No fixed release. rsa 0.9.10 via sqlx-mysql; rsa 0.10 rc via russh.
    "RUSTSEC-2023-0071",
]
