# Linux sprint: security, correctness and SQL character warnings

Committed and pushed as `30b7e530f` on `fork/linux`. Follow-up scope is the
[BookiE 0.1.1 review plan](bookie-0.1.1-plan.md); release gates below remain open.

Baseline: Linux `5dbf87a44`; macOS reference v0.72.0 `6e6396c59` through
`035ffe8f771797345ad86032e3aead16f18181fe` (remote verified 2026-09-14).
Linux is the delivery target. Reimplement relevant behavior in Rust/GTK;
never merge Apple source trees. Preserve existing manual-connection work.

## Ordered implementation checklist

- [x] P1: make Copy as IN clause dialect-aware using the shared literal renderer;
  disable it without a supported SQL dialect and report skipped values.
- [x] P1: correct MySQL escaped quotes and PostgreSQL nested comments in the
  shared lexer; cover escape strings, quoted identifiers and unterminated spans.
- [x] P1: isolate Redis browse database selection from normal commands, including
  cancellation, timeout, SELECT denial and reconnect.
- [x] P2: consolidate clipboard/page/file serializers; reserve duplicate column
  names, preserve fractional time and use the established `\x` binary encoding.
  Use spreadsheet-safe CSV by default with an explicit raw-text option.
- [x] P2: move file serialization and writes off GTK; write incrementally with
  progress/cancellation and private atomic staging; clearly label loaded rows.
- [x] P2: carry originating connection identity through committed DDL and refresh
  tables/views without applying stale results to another connection.
- [x] Feature: non-blocking SQL character warnings for full-width ASCII forms,
  curly quotes and unusual spaces. Skip lexical non-code spans; 250 ms debounce,
  at most 200 warnings, correct UTF-8/GTK offsets and safe editor teardown.

## Review evidence and acceptance

Baseline findings: IN-clause renderers omitted dialect-specific backslash escaping.
Lexer probes reproduced incorrect splitting of `SELECT 'a\';b'; SELECT 2`
(MySQL) and nested PostgreSQL comments containing semicolons. Redis temporary
SELECT restoration could be skipped when controlled execution dropped its future.
Export paths disagreed on duplicate names, bytes and fractional times; file writes
ran on GTK and bypassed formula-safe CSV options. Schema refresh lacked
originating connection identity and reloaded tables without views.

Each slice needs a regression before its fix and focused tests afterward.
Fixtures must prove literals stay data, Redis operations stay in their owning
database, failed/cancelled exports preserve destinations, delayed DDL results
cannot affect another connection, and warnings never rewrite executable text.
Test duplicate names `id,id,id_2`, NULL/empty bytes, fractional timestamps, wide
integers/decimals, adversarial CSV text, cancellation and stale async results.

## Upstream adoption decisions

| Reference | Decision |
| --- | --- |
| `c180dbcac` atomic/stoppable export | Adopt responsive incremental file writing; bulk dumps later |
| `558502f87` catalog refresh | Complete Linux connection-owned table/view refresh |
| `44c0ca4cb`, `04a7d22bb` scope/disconnect | Verify Linux ownership; fix Redis cancellation isolation |
| `c6265c1ba`, `3df2bb875` filters/reopen | Preserve existing Linux filters and connection-scoped reopen stack; further parity work deferred |
| `a45fb4672` SQL character warnings | Selected feature |
| View DDL, comments, materialized-view operations | Next candidate after stabilization |
| SQLite rebuild editing, bulk transfers, backup/restore | Separate transactional projects |
| Themes, maps, previews, highlight rules, new engines | Deferred behind existing-driver release evidence |
| Sparkle, Apple services, plugin ABI, entitlements | Excluded |

## Verification ledger

Review baseline: core export 16 passed; app export 6 passed; combined
core/policy/MCP command reported 398 passed and 1 ignored. Two new lexer probes
failed as expected. Cached dependency audit only reported allowed unmaintained
`paste`; its advisory data was dated 2026-08-20, so current safety was unverified.

Implementation checks apply to the working tree, not a published release:

- Full local CI passed: formatting, size/bounded-operation/panic guards, strict
  workspace Clippy, library/bin tests and sandbox integration (including the
  deterministic Redis cancellation fixture). Optional DuckDB is excluded by
  this existing gate. Core library: 249 passed; GTK app binaries: 228 passed.
- Policy administrative-dialect integration: 14 passed, including the new
  escaped-string/nested-comment read-only regression.
- Fresh RustSec audit and cargo-deny passed after replacing yanked transitive
  `chacha20` 0.10.1 with 0.10.2. Existing accepted `paste` maintenance advisory
  RUSTSEC-2024-0436 remains; duplicate-dependency warnings are not new failures.
- PostgreSQL release fixture passed: policy/SQL payloads, rollback, structure
  isolation, TLS identity/authority, SSH, reconnect and paging.
- Driver TLS fixture: 23 passed across ClickHouse, MongoDB, MySQL and Redis,
  including rejection of wrong/missing authorities and plaintext downgrade.
- Installed release GTK harness: all 18 scenarios passed, including unchanged
  Unicode SQL and exact warning selection, CREATE/DROP sidebar refresh, CSV/JSON
  files, approval denial/scope, restore, and multi-window connection ownership.
  Harness expectations were corrected for the existing Local policy and
  asynchronous AT-SPI actions; production policy was not relaxed.
- Driver integration: all 47 passed — PostgreSQL (9), driver/agent Unix sockets
  (2), MySQL (10), SQL Server (12), ClickHouse (13) and Redis (1). MySQL and
  ClickHouse executed the copied IN-clause payload regressions against real
  servers. Fixture teardown removed only disposable test containers/volumes.
- Deterministic Redis fixture passed cancellation, deadline, SELECT denial and
  a dropped selected socket; later commands and browse remain on their own DB.

Full-suite testing also exposed a MongoDB numeric conversion regression caused
by arbitrary-precision JSON serialization. Explicit JSON-to-BSON conversion now
preserves integer/filter semantics, with nested-value and wide-integer tests.
Structure editing without a rename now reports success and refreshes metadata.

The file export is incremental **over already loaded rows**, not an unbounded
database stream or snapshot. Cancellation is cooperative until atomic publish.
Catalog transaction tracking governs refresh notifications only; it does not
introduce a new editor transaction/session implementation. SQL warnings never
rewrite text or change policy classification.

Pending release gates: exact-candidate hosted CI (including Rust 1.98/MSRV and
optional DuckDB), Arch install/upgrade/rollback and the GTK soak ledger. Local
system Rust 1.98.1 is not MSRV evidence. No tagging, publishing or
production-readiness claim is part of this sprint. Existing manual-connection
files and old RC package artifacts are preserved, not regenerated or certified.
